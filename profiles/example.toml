# Copy this file to profiles/<project>.toml and fill in project-specific values.

name = ""
cwd = "/path/to/your/project"
model = ""
sandbox = ""
approval_policy = ""

# Optional: list files the context curator should read first.
rules = []

# Optional: constrain where the runner is allowed to operate.
# The runner also checks CODEX_HARNESS_WORKSPACE_ROOT.
allowed_paths = []

# Optional: commands the verifier can run or recommend for this project.
verification_commands = []

# Optional: route individual agent roles to model providers.
# [agent_models.planner]
# kind = ""
# model = ""
# reasoning_effort = ""
# verbosity = ""
